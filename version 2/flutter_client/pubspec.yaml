name: logistics_orchestrator_client
version: 0.1.0
publish_to: 'none'
description: Minimal Flutter demo consuming the FastAPI logistics backend with Leaflet/OSM via flutter_map.

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  http: ^1.1.0
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
